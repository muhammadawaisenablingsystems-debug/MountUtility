<Window x:Class="MountUtility.WPF.Views.Dialogs.ResizeDiskDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Resize Virtual Disk"
        Width="450" Height="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Resize Virtual Disk" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <TextBlock x:Name="DiskInfoText" Grid.Row="1" FontSize="14" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="New Size (MB)" Margin="0,0,0,5"/>
            <TextBox x:Name="NewSizeTextBox" Height="35" Padding="8"/>
            <TextBlock x:Name="CurrentSizeText" FontSize="12" Foreground="#666" Margin="0,5,0,0"/>
        </StackPanel>

        <StackPanel x:Name="PasswordPanel" Grid.Row="3" Margin="0,0,0,15" Visibility="Collapsed">
            <TextBlock Text="Password" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox" Height="35" Padding="8"/>
            <TextBlock Text="Password required to resize unmounted disk"
                       FontSize="11" Foreground="#666" Margin="0,5,0,0"/>
        </StackPanel>

        <TextBlock x:Name="ErrorMessage" Grid.Row="4" Foreground="Red"
                   TextWrapping="Wrap" Visibility="Collapsed"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Resize" Click="Resize_Click"
                    Style="{StaticResource PrimaryButton}"
                    Width="100" Height="35" Margin="0,0,10,0"/>
            <Button Content="Cancel" Click="Cancel_Click"
                    Style="{StaticResource SecondaryButton}"
                    Width="100" Height="35"/>
        </StackPanel>
    </Grid>
</Window>