<Window x:Class="MountUtility.WPF.Views.FirstRunWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Welcome to Disk Mount Utility"
        Width="500" Height="350"
        Icon="../Resources/app.ico"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Welcome to Disk Mount Utility"
                   FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Text="Choose your preferred key-exchange algorithm:"
                   FontSize="14" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <RadioButton x:Name="KyberRadio" GroupName="KexAlgo" Content="Kyber 1024 (Post-Quantum, Recommended)"
                         IsChecked="True" Margin="0,0,0,10" FontSize="13"/>
            <RadioButton x:Name="EcdhRadio" GroupName="KexAlgo" Content="ECDH P-256 (Classical, Faster)"
                         FontSize="13"/>
        </StackPanel>

        <TextBlock x:Name="ErrorMessage" Grid.Row="4" Foreground="Red"
                   TextWrapping="Wrap" Margin="0,0,0,15" Visibility="Collapsed"/>

        <Button Grid.Row="5" Content="Save and Continue"
                Click="SaveButton_Click"
                Style="{StaticResource PrimaryButton}"
                Height="40" FontSize="14"/>
    </Grid>
</Window>